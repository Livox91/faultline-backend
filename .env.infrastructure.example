# Copied automatically by `npm run setup`; replace placeholders only when creating
# configuration manually. Local services bind to loopback and use persistent volumes.
POSTGRES_USER=faultline
POSTGRES_PASSWORD=replace-with-generated-local-secret
POSTGRES_DB=faultline
POSTGRES_PORT=5432
REDIS_PORT=6379
NATS_PORT=4222
NATS_MONITOR_PORT=8222
CLICKHOUSE_USER=faultline
CLICKHOUSE_PASSWORD=replace-with-generated-local-secret
CLICKHOUSE_DB=faultline
CLICKHOUSE_HTTP_PORT=8123
